import { CheckCircle2, FileText, MessageSquare, ShieldCheck, Star } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { demoEvidence, demoTimeline } from '@/lib/demo-data'

const icons = { check: CheckCircle2, doc: FileText, star: Star, shield: ShieldCheck }
export default function EvidencePage() {
  return <div className="mx-auto max-w-5xl flex flex-col gap-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-primary">Evidence</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Your learning, in context.</h1><p className="mt-2 text-sm text-muted-foreground">A transparent record of the work behind every credential.</p></div><Button>Add evidence</Button></div><section className="rounded-2xl border border-border bg-card p-6"><h2 className="font-semibold">Eigenvector Centrality in Social Networks</h2><p className="mt-1 text-sm text-muted-foreground">8 evidence items · 4 verified · last updated today</p><div className="mt-8 flex flex-col gap-0">{demoTimeline.map((event, index) => { const Icon = icons[event.icon]; return <div key={event.id} className="flex gap-4"><div className="flex flex-col items-center"><div className="flex size-10 shrink-0 items-center justify-center rounded-full border border-primary/30 bg-primary/10"><Icon className="size-4 text-primary" /></div>{index < demoTimeline.length - 1 && <div className="w-px flex-1 bg-border" />}</div><div className="pb-8"><div className="flex flex-wrap items-center gap-2"><p className="font-medium text-foreground">{event.title}</p>{event.verified && <span className="text-xs text-primary">Verified</span>}</div><p className="mt-1 text-sm text-muted-foreground">{event.description}</p><p className="mt-2 text-xs text-muted-foreground">{event.date}</p></div></div> })}</div></section><section className="grid gap-4 sm:grid-cols-2">{demoEvidence.map((item) => <div key={item.id} className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between"><FileText className="size-5 text-primary" />{item.verified ? <CheckCircle2 className="size-4 text-primary" /> : <MessageSquare className="size-4 text-muted-foreground" />}</div><h3 className="mt-5 font-medium text-foreground">{item.title}</h3><p className="mt-2 text-sm leading-relaxed text-muted-foreground">{item.description}</p></div>)}</section></div>
}
