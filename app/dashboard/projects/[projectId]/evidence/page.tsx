import { BookOpen, CheckCircle2 } from "lucide-react"
export default async function ProjectEvidence({ params }: { params: Promise<{ projectId: string }> }) { const { projectId } = await params; return <main className="mx-auto flex max-w-5xl flex-col gap-6 p-6"><p className="text-sm font-medium text-primary">Evidence / {projectId}</p><h1 className="text-3xl font-semibold">Evidence timeline</h1>{["Project started", "Experiment recorded", "Parameter decision captured", "Reflection saved", "Peer feedback received"].map((event) => <div key={event} className="flex items-center gap-4 rounded-xl border border-border bg-card p-4"><div className="flex size-9 items-center justify-center rounded-full bg-primary/10 text-primary"><CheckCircle2 className="size-4" /></div><div><p className="font-medium">{event}</p><p className="text-sm text-muted-foreground">Learning evidence linked to your project.</p></div><BookOpen className="ml-auto size-4 text-muted-foreground" /></div>)}</main> }
