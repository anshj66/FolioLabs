import Link from "next/link"
import { ArrowRight, CheckCircle2, FlaskConical, ShieldCheck } from "lucide-react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Button, buttonVariants } from "@/components/ui/button"
import { cn } from "@/lib/utils"
export default async function ProjectPage({ params }: { params: Promise<{ projectId: string }> }) { const { projectId } = await params; return <main className="mx-auto flex w-full max-w-6xl flex-col gap-6 p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-primary">My Projects / {projectId}</p><h1 className="mt-2 text-3xl font-semibold">Eigenvector Exploration</h1><p className="mt-2 max-w-2xl text-muted-foreground">Investigate how matrix transformations affect vector direction and identify invariant directions.</p></div><span className="w-fit rounded-full bg-primary/10 px-3 py-1.5 text-sm font-medium text-primary">Evidence Developing</span></div><div className="grid gap-4 md:grid-cols-3">{[{ label: "Experiments", value: "12" }, { label: "Reflections", value: "5" }, { label: "Defense readiness", value: "82%" }].map((item) => <Card key={item.label}><CardContent className="p-5"><p className="text-sm text-muted-foreground">{item.label}</p><p className="mt-2 text-2xl font-semibold">{item.value}</p></CardContent></Card>)}</div><Card><CardHeader><CardTitle>Continue the proof-of-learning journey</CardTitle></CardHeader><CardContent className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[{ href: "lab", label: "Run experiments", icon: FlaskConical }, { href: "evidence", label: "View evidence", icon: CheckCircle2 }, { href: "reflection", label: "Write reflection", icon: ArrowRight }, { href: "defense", label: "Prepare defense", icon: ShieldCheck }].map((item) => <Link key={item.href} className={cn(buttonVariants({ variant: "outline" }), "justify-start gap-2")} href={`/dashboard/projects/${projectId}/${item.href}`}><item.icon className="size-4 text-primary" />{item.label}</Link>)}</CardContent></Card></main> }
