import { FlaskConical, ArrowUpRight } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
export default function ResearchPage() { return <main className="mx-auto flex w-full max-w-5xl flex-col gap-6 p-6"><div><p className="text-sm font-medium text-primary">Prove & Launch / Research</p><h1 className="mt-2 text-3xl font-semibold">Research potential</h1><p className="mt-2 text-muted-foreground">Promising learning evidence can become a new question worth pursuing.</p></div><Card><CardHeader><CardTitle className="flex items-center gap-2"><FlaskConical className="size-5 text-primary" />Eigenvector Stability Under Nonlinear Transformations</CardTitle></CardHeader><CardContent className="flex flex-col gap-4"><p className="leading-relaxed text-muted-foreground">A faculty-flagged extension of your current project. Investigate whether invariant directions remain stable when the transformation is no longer linear.</p><div className="grid gap-3 sm:grid-cols-3"><div><p className="text-xs text-muted-foreground">Faculty</p><p className="mt-1 text-sm font-medium">Dr. Sarah Chen</p></div><div><p className="text-xs text-muted-foreground">Status</p><p className="mt-1 text-sm font-medium text-primary">Potential research project</p></div><div><p className="text-xs text-muted-foreground">Evidence</p><p className="mt-1 text-sm font-medium">12 experiments linked</p></div></div><Button className="self-start">Open research brief <ArrowUpRight data-icon="inline-end" /></Button></CardContent></Card></main> }
