import { ArrowUpRight, CheckCircle2, ExternalLink, ShieldCheck } from 'lucide-react'
import { Button, buttonVariants } from '@/components/ui/button'
import { cn } from '@/lib/utils'
import { demoProjects, demoReviews, demoUser } from '@/lib/demo-data'
export default function PortfolioPage() { return <div className="mx-auto max-w-6xl flex flex-col gap-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-primary">Public portfolio</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{demoUser.name}&apos;s verified work</h1><p className="mt-2 text-sm text-muted-foreground">A living record of projects, evidence, and the people who can vouch for it.</p></div><a href="#preview" className={cn(buttonVariants({ variant: 'outline' }), 'gap-2')}>Preview public page <ExternalLink className="size-4" /></a></div><section className="grid gap-6 lg:grid-cols-[0.75fr_1.25fr]"><div className="rounded-2xl border border-border bg-card p-6"><div className="flex size-16 items-center justify-center rounded-full bg-primary/10 text-xl font-semibold text-primary">AM</div><h2 className="mt-5 text-xl font-semibold">{demoUser.name}</h2><p className="mt-1 text-sm text-primary">Computational Mathematics · MIT</p><p className="mt-5 text-sm leading-relaxed text-muted-foreground">{demoUser.bio}</p><div className="mt-8 flex items-center gap-3 rounded-xl border border-primary/20 bg-primary/5 p-4"><ShieldCheck className="size-6 text-primary" /><div><p className="font-semibold text-foreground">94% authenticity</p><p className="text-xs text-muted-foreground">Across 5 verified portfolio entries</p></div></div></div><div id="preview" className="flex flex-col gap-4">{demoProjects.map((project) => <article key={project.id} className="rounded-2xl border border-border bg-card p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs uppercase tracking-[0.16em] text-primary">{project.category}</p><h2 className="mt-2 text-xl font-semibold">{project.name}</h2></div>{project.status === 'completed' && <CheckCircle2 className="size-5 text-primary" />}</div><p className="mt-3 max-w-2xl text-sm leading-relaxed text-muted-foreground">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.tags.map((tag) => <span key={tag} className="rounded-full bg-secondary px-3 py-1 text-xs text-muted-foreground">{tag}</span>)}</div><div className="mt-6 flex items-center justify-between border-t border-border pt-4"><span className="text-xs text-muted-foreground">{project.progress}% documented</span><Button variant="ghost" size="sm">Explore project <ArrowUpRight className="size-4" /></Button></div></article>)}</div></section><section className="rounded-2xl border border-border bg-card p-6"><h2 className="font-semibold">Verification panel</h2><p className="mt-1 text-sm text-muted-foreground">Reviewers who have verified Alex&apos;s work</p><div className="mt-6 flex flex-wrap gap-4">{demoReviews.map((review) => <div key={review.id} className="flex items-center gap-3 rounded-xl bg-secondary/40 p-4"><div className="flex size-9 items-center justify-center rounded-full bg-primary/10 text-xs font-semibold text-primary">{review.reviewer.name.split(' ').map((part) => part[0]).join('').slice(0, 2)}</div><div><p className="text-sm font-medium">{review.reviewer.name}</p><p className="text-xs text-muted-foreground">Verified reviewer · {review.timestamp}</p></div></div>)}</div></section></div> }
