"use client"
import { useState } from "react"
import { CheckCircle2 } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
export default function PeerReviewPage() { const [submitted, setSubmitted] = useState(false); return <main className="mx-auto flex w-full max-w-4xl flex-col gap-6 p-6"><div><p className="text-sm font-medium text-primary">Collaborate & Validate / Peer Review</p><h1 className="mt-2 text-3xl font-semibold">Review a project</h1><p className="mt-2 text-muted-foreground">Give specific feedback that helps another learner strengthen their evidence.</p></div><Card><CardHeader><CardTitle>Anonymous project: Spectral clustering</CardTitle></CardHeader><CardContent className="flex flex-col gap-4">{["Clarity", "Reasoning", "Experimental design", "Evidence", "Communication"].map((label) => <label key={label} className="flex items-center justify-between border-b border-border pb-3 text-sm">{label}<select className="rounded-md border border-border bg-background px-3 py-2" defaultValue="4"><option value="3">3 / 5</option><option value="4">4 / 5</option><option value="5">5 / 5</option></select></label>)}{submitted ? <p className="flex items-center gap-2 text-sm text-primary"><CheckCircle2 className="size-4" />Review saved as validation evidence.</p> : <Button onClick={() => setSubmitted(true)} className="self-start">Submit peer review</Button>}</CardContent></Card></main> }
