import { CheckCircle2, ExternalLink, ShieldCheck, Star } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { facultyProfiles, demoReviews } from '@/lib/demo-data'
export default function FacultyPage() { return <div className="mx-auto max-w-5xl flex flex-col gap-8"><div><p className="text-sm text-primary">Faculty network</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Verification you can stand behind.</h1><p className="mt-2 text-sm text-muted-foreground">See who has reviewed your work and how your evidence becomes a trusted credential.</p></div><section className="grid gap-4 sm:grid-cols-2">{facultyProfiles.map((faculty) => <div key={faculty.id} className="rounded-2xl border border-border bg-card p-6"><div className="flex items-start justify-between"><div className="flex size-12 items-center justify-center rounded-full bg-primary/10 text-lg font-semibold text-primary">{faculty.name.split(' ').map((part) => part[0]).join('').slice(0, 2)}</div>{faculty.verified && <ShieldCheck className="size-5 text-primary" />}</div><h2 className="mt-5 font-semibold">{faculty.name}</h2><p className="mt-1 text-sm text-muted-foreground">{faculty.title}</p><p className="mt-3 text-sm text-primary">{faculty.specialty}</p><p className="mt-5 text-xs text-muted-foreground">{faculty.projectsReviewed} projects reviewed</p></div>)}</section><section className="rounded-2xl border border-border bg-card"><div className="border-b border-border p-6"><h2 className="font-semibold">Your verified reviews</h2><p className="mt-1 text-sm text-muted-foreground">Faculty feedback attached to your project record</p></div>{demoReviews.map((review) => <div key={review.id} className="flex flex-col gap-4 border-b border-border p-6 last:border-0 sm:flex-row sm:items-start"><div className="flex size-10 shrink-0 items-center justify-center rounded-full bg-primary/10 text-sm font-semibold text-primary">{review.reviewer.name.split(' ').map((part) => part[0]).join('').slice(0, 2)}</div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><p className="font-medium">{review.reviewer.name}</p><span className="flex items-center gap-1 text-xs text-primary"><CheckCircle2 className="size-3" /> Verified</span></div><div className="mt-2 flex gap-1">{Array(review.rating).fill(0).map((_, i) => <Star key={i} className="size-3 fill-primary text-primary" />)}</div><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{review.feedback}</p></div><Button variant="outline" size="sm"><ExternalLink className="size-4" />View evidence</Button></div>)}</section></div> }
